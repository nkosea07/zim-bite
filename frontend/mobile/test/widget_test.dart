import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // TODO: Add smoke test once BLoC dependencies are wired with mocks
    expect(true, isTrue);
  });
}
